# Copy to secrets.toml only for local values that should not be committed.
# User passwords are created in the application and are not stored here.

demo_mode = false

[telegram]
bot_token = "replace-with-token-from-botfather"
# approved_group_id = -1001234567890

# Optional: natural-language Telegram intake. Without it the bot
# still works using the button flow only.
[gemini]
api_key = "replace-with-key-from-google-ai-studio"
