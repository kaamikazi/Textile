# Al Sadi Knitwear Factory OS

Modern Streamlit-based knitting factory management system using Python, SQLite, and Plotly.

## Run

```powershell
pip install -r requirements.txt
streamlit run app.py
```

The app initializes `factory.db` automatically with production-ready tables and starter records.
